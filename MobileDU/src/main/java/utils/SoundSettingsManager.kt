package utils

object SoundSettingsManager {
    var mute: Boolean = false
    var volume: Int = 50 // Pode ser a configuração padrão do sistema
    var screenReaderEnabled: Boolean = false
    var speakingSpeed: Float = 1.0f
}